# Windows Setup

Use PowerShell for the Python environment:

```powershell
scripts\setup_windows.ps1
```

Install COLMAP and Vulkan-capable GPU drivers separately. Use Git Bash or a manual clone for VkSplat if needed.
